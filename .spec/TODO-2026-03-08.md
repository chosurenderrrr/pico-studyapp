# TODO

## タスク一覧

### 機能A: カレンダーメモのタイトル/内容分離
- [x] CSS：カレンダーセル内メモタイトル用スタイルを追加（`.cal-memo-title`）
- [x] HTML：メモ入力エリアをタイトル入力＋内容入力の2段に変更
- [x] `Cal.render()` の日付セル生成を改修：●ドット削除 → タイトルテキスト表示（後方互換あり）
- [x] `Cal.selectDate()` を改修：旧`memo`フィールドの後方互換読み込み
- [x] `Cal.saveMemo()` を改修：`memoTitle` / `memoContent` を個別保存

### 機能B: 学習統計の拡充
- [x] `Quiz` オブジェクトにセッション開始時刻・問題開始時刻・problemTimesを追加
- [x] `Quiz.start()` でセッション開始時刻を記録
- [x] `Quiz.render()` で問題開始時刻を記録
- [x] `Quiz.answer()` で問題解答時間を計算・記録
- [x] `Data.recordResult()` に解答時間パラメータを追加（totalTime / answerCount）
- [x] `Quiz.finish()` に `duration` と `problemTimes` を追加してstudy-logへ保存
- [x] カレンダーセッション記録に `duration` を追加
- [x] `Cal.selectDate()` の学習記録表示にセッション時間を追加
- [x] `Stats.calcStreak()` 実装（連続学習日数）
- [x] `Stats.fmtDuration()` 実装（時間フォーマット）
- [x] `Stats.render()` の全体カードに「総勉強時間」「連続学習日数」を追加
- [x] `Stats.render()` のセット別カードに「セッション平均時間」を追加
- [x] `Stats.render()` に苦手問題ランキング（上位3）と解答時間ランキング（上位3）を追加
