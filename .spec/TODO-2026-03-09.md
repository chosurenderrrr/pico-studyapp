# TODO

## タスク一覧

### Phase 1: データ層（ProblemIO）
- [x] `ProblemIO.load()` に `title.txt` 読み込みを追加（なければ空文字）
- [x] `ProblemIO.load()` の `multiple-choice` 処理を変更：`correct1〜N.txt` / `wrong1〜N.txt` の連番読み込みに変更
- [x] `ProblemIO.load()` に `display_count.txt` 読み込みを追加（なければ全選択肢数をデフォルトに）
- [x] `ProblemIO.save()` に `title.txt` 書き込みを追加（空文字はスキップ）
- [x] `ProblemIO.save()` の `multiple-choice` 処理を変更：`correct1〜N.txt` / `wrong1〜N.txt` 連番書き込みに変更
- [x] `ProblemIO.save()` に `display_count.txt` 書き込みを追加

### Phase 2: CSS
- [x] タイトル入力欄のスタイルを追加
- [x] エディタの「正解プール」「誤答プール」「表示数」セクションのスタイルを追加
- [x] 複数選択クイズの選択肢ボタン（選択中ハイライト）スタイルを追加
- [x] フィードバック時の色分けスタイルを追加（緑/赤/黄/グレー）

### Phase 3: エディタ（Editor）
- [x] エディタHTML最上部にタイトル入力欄（`#problem-title`）を追加
- [x] エディタHTMLの「問題タイプ」タブのラベルを「選択問題」に変更
- [x] エディタHTMLの選択問題エリアを「正解プール」「誤答プール」「出題時の表示数」に全面改修
- [x] `Editor.load()` にタイトル読み込み処理を追加
- [x] `Editor.load()` の `multiple-choice` 読み込み処理を変更（`corrects[]` / `wrongs[]` / `displayCount`）
- [x] `Editor.save()` にタイトル収集処理を追加
- [x] `Editor.save()` の `multiple-choice` 収集処理を変更（バリデーション含む）
- [x] `Editor.addCorrectItem()` を実装（正解を1行追加）
- [x] `Editor.removeCorrectItem(btn)` を実装（正解行を削除）
- [x] `Editor.addWrongItem()` を実装（誤答を1行追加）
- [x] `Editor.removeWrongItem(btn)` を実装（誤答行を削除）

### Phase 4: クイズ画面（Quiz）
- [x] クイズHTMLに複数選択用エリアを追加（`#mc-confirm-btn`）
- [x] `Quiz.renderMultipleChoice(p)` を全面改修：全正解 + ランダム誤答をシャッフル表示
- [x] 選択肢タップでトグル（選択中ハイライト）を実装
- [x] `Quiz.confirmMultipleChoice()` を実装（選択セット = 全正解 かで判定）
- [x] 不正解フィードバック：各選択肢を4色で色分け（緑/赤/黄/グレー）
- [x] `Data.recordResult()` の呼び出しを維持・調整

### Phase 5: 問題選択画面（App）
- [x] `App.renderProblemList()` の表示を変更：`title` があればタイトル表示、なければ問題文先頭20文字

### Phase 6: 動作確認
- [ ] 選択問題（単一正解）の作成・保存・出題・正誤判定が正常に動作する
- [ ] 選択問題（複数正解）の出題・判定（全選択で正解）が正常に動作する
- [ ] 誤答プールからランダムに選ばれることを確認
- [ ] タイトル設定あり・なしで問題選択画面の表示を確認
- [ ] 整序問題と選択問題が同一セット内に混在して出題される
- [ ] 苦手モード・前回ミスモードで正常に絞り込まれる

---

## 完了済み（前サイクル）

### Phase 1〜6: 整序問題（order タイプ）
- [x] `ProblemIO.load()` / `save()` の整序問題対応
- [x] 整序問題エディタ UI
- [x] 整序問題クイズ画面（シャッフル表示・スロット操作・正誤判定・フィードバック）
- [x] 問題一覧への🔢アイコン表示
